---

- name: Change admin password MikroTik MPR
  hosts: GALA_MikroTik_MPR
  gather_facts: false
  connection: network_cli

  tasks:
    - name: Note
      debug:
        msg:
          - {{adm_password}}
