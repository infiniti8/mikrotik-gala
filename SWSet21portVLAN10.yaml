- name: SWITCH VLAN
  hosts: all
  gather_facts: false
  connection: local
  tasks:
    - name: VLAN default port DELETED
      telnet:
        user: admin
        password: Crkthjp2
        login_prompt: "UserName: "
        prompts:
          - "[>|#]"
        command:
          - config vlan default delete 21
          - config vlan Data add untagged 21
          - save