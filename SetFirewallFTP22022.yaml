- name: Change FTP Firewall rule
  hosts: GALA_MikroTik_MPR
  gather_facts: false
  connection: network_cli
  vars:
    ansible_command_timeout: 180
  tasks:
    - name: Change rule
      routeros_command:
        commands:
          - /ip firewall filter set [find comment=FTP] dst-port=21,22022
